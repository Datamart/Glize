name: schedule

on:
  schedule:
    - cron: '*/15 * * * *'

jobs:
  init:
    # runs-on: ubuntu-latest
    runs-on: macos-latest
    steps:
      - run: npm init --force
      - run: npm install glize komito-analytics
      - run: cd node_modules/glize/tests
      - run: node index.js
